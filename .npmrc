registry=https://registry.npmjs.org/
# Auth: set NODE_AUTH_TOKEN env or use local .npmrc (do not commit tokens)
# Host (e.g. API) provides typeorm; don't install peer deps here to avoid duplicate
omit=peer
